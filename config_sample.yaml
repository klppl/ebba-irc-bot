server: irc.server.org
port: 6667
use_tls: false
nickname: ebba
username: ebba
realname: Ebba Busch
channels:
- '#kristdemokraterna'
prefix: .
owner_nicks:
- nick: owner
  password: change-me
  hosts:
  - '~ident@trusted.host'
reconnect_delay_secs: 5
request_timeout_secs: 10
# SASL PLAIN authentication (optional). Negotiated via IRCv3 CAP.
# sasl: true
# sasl_username: ebba          # defaults to username/nickname if omitted
# sasl_password: change-me
# Plugins are enabled by default. To disable a plugin:
# plugins:
#   plugin_name:
#     enabled: false
#
# Cost-optimized OpenAI configuration (the key may instead be set in OPENAI_API_KEY):
# plugins:
#   ai:
#     enabled: true
#     provider: openai
#     api_key: ""
#     model: gpt-5.6-luna
#     language: en
#     reasoning_effort: none
#     search_max_calls: 1
#     search_context_size: low
#     history_context_entries: 8
#     background_context_chars: 1200
#     max_reply_chars: 420
#     chimein_enabled: true  # master switch; use .talkback on per channel
#     store_responses: false
#     history_retention_days: 30
#     history_max_entries: 100
#     connect_timeout_secs: 10
#     request_timeout_secs: 90
#     api_attempts: 2
#     failure_threshold: 5
#     failure_cooldown_secs: 60
